<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="8dp">

    <TextView
        android:id="@+id/chargeNameText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/my_light_on_surface"
        android:textSize="16sp"
        android:fontFamily="@font/montserratregular"
        android:text="@string/extra_charge_type"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/chargeAmountText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/chargeAmountText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/extra_charge_amount"
        android:layout_marginEnd="8dp"
        android:textColor="@color/my_light_primary"
        android:fontFamily="@font/montserratsemibold"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/deleteChargeButton"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/deleteChargeButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Delete charge"
        android:src="@drawable/material_symbols__delete"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/my_light_error" />

</androidx.constraintlayout.widget.ConstraintLayout>