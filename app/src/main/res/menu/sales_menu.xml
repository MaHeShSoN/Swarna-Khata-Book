<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/bx__search_alt_2"
        android:title="Search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />

    <item
        android:id="@+id/action_filter"
        android:icon="@drawable/ic_filter"
        android:title="Filter"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_sort"
        android:icon="@drawable/bi__three_dots_vertical"
        android:title="Sort"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/sort_date_newest"
                android:title="Date (Newest First)" />
            <item
                android:id="@+id/sort_date_oldest"
                android:title="Date (Oldest First)" />
            <item
                android:id="@+id/sort_amount_high"
                android:title="Amount (High to Low)" />
            <item
                android:id="@+id/sort_amount_low"
                android:title="Amount (Low to High)" />
        </menu>
    </item>
</menu>